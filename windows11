<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Windows 11 Emulator</title>
  <style>
    body {
      margin: 0;
      background: url('https://wallpaperaccess.com/full/5690544.jpg') no-repeat center center fixed;
      background-size: cover;
      font-family: 'Segoe UI', sans-serif;
    }
    .taskbar {
      position: fixed;
      bottom: 0;
      width: 100%;
      height: 50px;
      background: rgba(30, 30, 30, 0.8);
      display: flex;
      justify-content: center;
      align-items: center;
      backdrop-filter: blur(10px);
    }
    .start-button {
      width: 40px;
      height: 40px;
      background: url('https://upload.wikimedia.org/wikipedia/commons/5/5f/Windows_logo_-_2021.svg') no-repeat center;
      background-size: contain;
      border-radius: 8px;
      cursor: pointer;
    }
    .start-menu {
      position: fixed;
      bottom: 60px;
      left: 50%;
      transform: translateX(-50%);
      width: 300px;
      background: rgba(255, 255, 255, 0.9);
      border-radius: 10px;
      padding: 20px;
      display: none;
      box-shadow: 0 0 10px rgba(0,0,0,0.3);
    }
  </style>
</head>
<body>
  <div class="taskbar">
    <div class="start-button" onclick="toggleStartMenu()"></div>
  </div>
  <div class="start-menu" id="startMenu">
    <h3>Start Menu</h3>
    <p>Apps, Settings, Power...</p>
  </div>

  <script>
    function toggleStartMenu() {
      const menu = document.getElementById('startMenu');
      menu.style.display = menu.style.display === 'none' ? 'block' : 'none';
    }
  </script>
</body>
</html>
